# Example environment variables for OpenBoards
# Copy this file to .env.local and fill in the values

# Required: Postgres connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/openboards

# Optional: absolute base URL for server-side fetches (leave empty to use relative)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
