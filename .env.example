# Database
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME

# Better Auth
BETTER_AUTH_SECRET=replace-with-a-long-random-string
BETTER_AUTH_URL=http://localhost:3000

# App
ROOT_DOMAIN=openboards.co
# Custom domain support

# Root domain for subdomain routing (middleware)
ROOT_DOMAIN=openboards.co
# Root domain exposed to client
NEXT_PUBLIC_ROOT_DOMAIN=openboards.co

# Vercel optional integration (add domains to project)
#VERCEL_TOKEN=
#VERCEL_PROJECT_ID=
#VERCEL_TEAM_ID=
