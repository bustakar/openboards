# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Auth (better-auth)
AUTH_SECRET=change-me-32+chars

# OAuth
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/openboards

# Email (magic link via SMTP; use Mailpit locally)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="OpenBoards <noreply@localhost>"

# Cookies (HMAC for anonymous visitorId)
COOKIE_SIGNING_SECRET=change-me-32+chars

# Stripe (hosted mode only)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=